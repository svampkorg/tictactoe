#include "../headers/helpers.h"
using namespace std;

// static void draw_board(const vector<bool> &board) {
//   stringstream ss;
//
//   // for_each(board.begin(), board.end(), [](const optional<bool> &spot) {
//   //   if (spot.has_value()) {
//   //     // NOTE: Either X or 0
//   //     if (spot.value()) {
//   //       // NOTE: X
//   //     } else {
//   //       // NOTE: 0
//   //     }
//   //   } else {
//   //     // NOTE: Empty spot
//   //   }
//   // });
//
//   for (int i = 0; i < board.size(); ++i) {
//     const auto &place = board[i];
//
//     if (i % 3 == 0) {
//       ss << endl;
//     }
//
//     if (place == true) {
//       ss << "[o]";
//     } else if (place == false) {
//       ss << "[x]";
//     } else {
//       ss << "[ ]";
//     }
//   }
//
//   cout << ss.str();
// }
